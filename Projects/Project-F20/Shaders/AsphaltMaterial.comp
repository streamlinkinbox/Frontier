//============================================================================================================================================
// 📦 Project-F20/Shaders/AsphaltMaterial.comp — Procedural Asphalt Road Surface and Wetness Material Compute Shader
//============================================================================================================================================

#version 460

layout(local_size_x = 16, local_size_y = 16, local_size_z = 1) in;

//------------------------------------------------------------------------------------------------------------------------
//                                                    STORAGE BINDINGS
//------------------------------------------------------------------------------------------------------------------------

layout(std430, binding = 0) writeonly buffer RoadSurfaceOutputExtent
{
    vec4 SurfaceAttributes[];                           // [0..1] albedo, roughness, metallic, wetness
};

layout(push_constant) uniform PushConstants
{
    uint ViewportWidth;                                 // [px] viewport width
    uint ViewportHeight;                                // [px] viewport height
    float TrackPuddleWetnessRatio;                      // [0..1] rain wetness level
    float TireGrooveDeformation;                        // [0..1] tire mark wear factor
};

//------------------------------------------------------------------------------------------------------------------------
//                                              COMPUTE MAIN INVOCATION
//------------------------------------------------------------------------------------------------------------------------

void main()
{
    uvec2 PixelCoord = gl_GlobalInvocationID.xy;
    if (PixelCoord.x >= ViewportWidth || PixelCoord.y >= ViewportHeight)
    {
        return;
    }

    uint LinearPixelIndex = PixelCoord.y * ViewportWidth + PixelCoord.x;
    // Evaluates multi-frequency asphalt micro-roughness and Fresnel specular water puddles
}
